@using WebUI.Client.Components

<MudDialog>
    <DialogContent>
        @if (ErrorVisible)
        {
            <MudAlert Severity="Severity.Error">@Localizer["AnErrorOccuredDuringSaving"]</MudAlert>
        }
        <div class="row">
            <div class="col">
                <EditForm Model=@UpdateInstructorInputModel OnSubmit=@FormSubmitted>
                        <FluentValidationValidator DisableAssemblyScanning="true" />
                        <div class="form-group">
                            <label for="LastName">@Localizer["LastName"]</label>
                            <InputText @bind-Value=UpdateInstructorInputModel.LastName class="form-control" id="LastName" />
                            <ValidationMessage For="() => UpdateInstructorInputModel.LastName" />
                        </div>
                        <div class="form-group">
                            <label for="FirstName">@Localizer["FirstName"]</label>
                            <InputText @bind-Value=UpdateInstructorInputModel.FirstName class="form-control" id="FirstName" />
                            <ValidationMessage For="() => UpdateInstructorInputModel.FirstName" />
                        </div>
                        <div class="form-group">
                            <label for="HireDate">@Localizer["HireDate"]</label>
                            <InputDate @bind-Value=UpdateInstructorInputModel.HireDate class="form-control" id="HireDate" />
                            <ValidationMessage For="() => UpdateInstructorInputModel.HireDate" />
                        </div>
                        <div class="form-group">
                            <label for="OfficeLocation">@Localizer["OfficeLocation"]</label>
                            <InputText @bind-Value=UpdateInstructorInputModel.OfficeLocation class="form-control" id="OfficeLocation" />
                            <ValidationMessage For="() => UpdateInstructorInputModel.OfficeLocation" />
                        </div>
                        <div>
                            <InputFile id="fileInput" OnChange="UploadFiles" hidden />
                            <MudButton HtmlTag="label"
                                       Color="Color.Primary"
                                       StartIcon="@Icons.Material.Filled.CloudUpload"
                                       for="fileInput">
                                @Localizer["UploadFiles"]
                            </MudButton>
                            @if (File != null)
                            {
                                <MudList>
                                    <MudListItem Icon="@Icons.Material.Filled.AttachFile" @key="@File">
                                            @File.Name <code>@File.Size bytes</code>
                                    </MudListItem>
                                </MudList>
                            }
                        </div>
                        <MudButton ButtonType="ButtonType.Submit"
                                   StartIcon="fas fa-save"
                                   Color="Color.Primary">@Localizer["Save"]</MudButton>
                        <MudButton Color="Color.Primary" OnClick="Cancel">@Localizer["Cancel"]</MudButton>
                    </EditForm>
            </div>
            <div class="col">
                <ProfilePicture FileName="@UpdateInstructorInputModel?.ProfilePictureName" />
            </div>
        </div>
    </DialogContent>
</MudDialog>